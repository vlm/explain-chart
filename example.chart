
/*
 * By default, we treat both axis ranges as [0..10].
 */
x-axis range 0..5 unlabeled
y-axis range 0..5 unlabeled

/*
 * Define two lines.
 */
A = line with slightly negative slope
B = horizontal line

/*
 * Say that these lines intersect at a particular point.
 */
A and B intersect at (2, 2)

/*
 * You can also add more intersection points, as well as more shape names:
 */
// A and B and C intersect at (2, 2) and (5, 3)

